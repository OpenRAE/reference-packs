schema_version: environment-kit-seed/v1
kit: infrastructure.linux-domain-member
configuration:
  computer_name: linux-member-01
  deployment_profile: standard
  service_label: linux-domain-member
declared_objects:
- id: domain_join_profile
  kind: infrastructure-seed
  sensitivity: non-secret
- id: nss_mapping
  kind: infrastructure-seed
  sensitivity: non-secret
- id: sudo_policy
  kind: infrastructure-seed
  sensitivity: non-secret
integration_requirements:
- Compose with one exported RAES identity domain and add the pack-level domain-join
  relationship.
